{
  "name": "vodplayer",
  "version": "1.0.0",
  "description": "Video player",
  "main": "VODPlayer.ts",
  "scripts": {
    "clean": "rimraf ./dist/VODPlayer.d.ts && rimraf ./dist/VODPlayer.js",
    "prebuild": "npm run clean",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yangfan",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^2.6.3",
    "typescript": "^3.5.3"
  }
}
